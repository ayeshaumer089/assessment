# =========================
# Backend Environment (Example)
# =========================
# Copy this file to `Backend/.env` and replace placeholder values.
#
# IMPORTANT:
# - Do NOT commit real secrets to git.
# - For Gmail SMTP, use an "App Password" (not your normal password).

# --- Server ---
PORT=5000
NODE_ENV=development

# --- Database ---
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# --- Auth ---
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=1d

# --- Google Auth (optional) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=postmessage

# --- Password Reset Flow ---
# Frontend base URL used to generate the reset link sent via email.
FRONTEND_BASE_URL=http://localhost:5173

# Optional: dedicated secret for password reset tokens. If empty, JWT_SECRET is used.
PASSWORD_RESET_TOKEN_SECRET=

# Token expiry (examples: 15m, 1h)
PASSWORD_RESET_TOKEN_EXPIRES_IN=15m

# --- Email (SMTP) ---
# Gmail SMTP example:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourgmail@gmail.com
# SMTP_PASS=your-gmail-app-password
# SMTP_FROM="NexusAI <yourgmail@gmail.com>"
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
